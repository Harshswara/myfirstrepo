import { api, LightningElement, track } from 'lwc';

export default class Parentperson1 extends LightningElement {
    updateuser()
    {
        this.template.queryDecorator("c-person1".updateuser())
    }
}